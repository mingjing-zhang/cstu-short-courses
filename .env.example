# Optional. Without this key the AI advisor falls back to a built-in rule-based
# recommender, so the demo runs out of the box. Drop a real key here to make the
# advisor call Claude live.
ANTHROPIC_API_KEY=
